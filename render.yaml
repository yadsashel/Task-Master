services:
  - type: web
    name: taskmaster
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:5000"
    plan: free
    runtime: python3